name: Release

on:
  push:
    branches: [main]

concurrency: release-${{ github.ref }}

permissions:
  contents: write
  pull-requests: write
  # Required for npm provenance attestations.
  id-token: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          registry-url: https://registry.npmjs.org

      - run: npm ci
      - run: npm test

      # Opens (or updates) a release PR when changesets are pending;
      # publishes to npm when that PR is merged.
      - uses: changesets/action@v1
        with:
          publish: npm run release
          title: "chore: release"
          commit: "chore: release"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # npm reads this through the .npmrc setup-node wrote. The changesets action
          # looks for NPM_TOKEN instead, and without it announces "No NPM_TOKEN found,
          # but OIDC is available - using npm trusted publishing" — a path that needs a
          # trusted publisher configured on npmjs.com, which this package does not have.
          # Both names are set so neither layer has to guess.
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
          NPM_CONFIG_PROVENANCE: "true"
